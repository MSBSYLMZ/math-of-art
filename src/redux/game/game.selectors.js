export const selectGame = ((state)=>state.game);

export const selectNewGame = (state=>state.game.newGame)

export const selectMessage = (state=>state.game.message)

export const selectMessageType = (state => state.game.messageType)